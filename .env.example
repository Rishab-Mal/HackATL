# OpenRouter API key — get one at https://openrouter.ai
OPENROUTER_API_KEY=sk-or-v1-...

# Backend database — Supabase (Postgres).
# Copy the "Connection string" from Supabase: Project Settings → Database →
# Connection string → URI (use the Session/Transaction pooler), and paste your
# database password in place of [YOUR-PASSWORD].
DATABASE_URL=postgresql://postgres.eamseugexmiorqbzxxek:[YOUR-PASSWORD]@aws-0-us-east-1.pooler.supabase.com:6543/postgres

# Frontend (Vite exposes vars prefixed with VITE_)
VITE_API_BASE_URL=http://localhost:8000
